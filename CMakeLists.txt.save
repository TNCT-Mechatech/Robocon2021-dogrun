cmake_minimum_required(VERSION 3.0.2)
project(dogrun)

find_package(catkin REQUIRED COMPONENTS
  cv_bridge
  geometry_msgs
  image_transport
  roscpp
  sensor_msgs
  std_msgs
)

catkin_package(
)

find_package(OpenCV REQUIRED)

include_directories(
 ${catkin_INCLUDE_DIRS} ${OpenCV_INCLUDE_DIRS}
)

add_library(recognize_circle_lib src/circle.cpp)
add_executable(recognize_circle src/circle.cpp)

target_link_libraries(
 recognize_circle
 ${catkin_LIBRARIES}
 ${OpenCV_LIBRARIES}
)

add_library(twist_serial_lib src/twist_serial.cpp)
add_executable(twist_serial src/twist_serial.cpp)

target_link_libraries(
 twist_serial
 ${catkin_LIBRARIES}
 #${CMAKE_CURRENT_SOURCE_DIR}/SerialBridge/bin/libSerialBridge.a
 #${CMAKE_SOURCE_DIR}/SerialBridge/bin/libSerialBridge.a
)

target_link_libraries(twist_serial ${CMAKE_CURRENT_SOURCE_DIR}/SerialBridge/bin/libSerialBridge.a)

#target_include_directories(twist_serial PUBLIC ${CMAKE_CURRENT_SOURCE_DIR}/SerialBridge/bin/libSerialBridge.a)

#target_include_directories(twist_serial PUBLIC ${CMAKE_CURRENT_SOURCE_DIR}/SerialBridge/bin/libSerialBridge.a)
